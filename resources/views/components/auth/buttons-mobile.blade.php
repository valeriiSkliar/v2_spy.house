@guest

<div class="mb-15">
    <a href="{{ route('login') }}" class="btn _flex w-100 mb-10 _dark">{{ __('auth.login') }}</a>
    <a href="{{ route('register') }}" class="btn _flex w-100 mb-10 _green">{{ __('auth.register') }}</a>
</div>
@endguest